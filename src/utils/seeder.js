// Dummy data seeder disabled. Real MQTT device telemetry and database records are preserved.

export const ensureInfluxData = async () => {
  // No dummy timeseries data seeding
  return;
};

export const seedDatabaseProgrammatically = async () => {
  // No dummy database records seeding
  return;
};
